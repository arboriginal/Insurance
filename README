Insurance is a plugin for Bukkit (Minecraft server).
All you need to run it is to copy the jar file into the "plugins" directory.

Then, start your server. Default config file will be created into
plugins/Insurance. Read the doc, modify your configuration and (if you are op) run the command /insurance-reload or restart the server.